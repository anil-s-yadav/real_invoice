org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false

# Fix for Windows cross-drive Kotlin incremental compilation cache bug
# Project is on D: drive while Pub cache is on C: drive
kotlin.incremental=false
kotlin.incremental.java=false

# Route Gradle cache, dependencies, and native transforms to D: drive with 264 GB free space
systemProp.gradle.user.home=D:/.gradle
